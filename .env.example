# Public frontend configuration
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Optional public OAuth identifier. Never place the client secret or tokens here.
PUBLIC_GOOGLE_CLIENT_ID=

# Supabase Edge Function secrets — configure in Supabase, never expose in the frontend
APP_ORIGIN=
GEMINI_API_KEY=
OCR_MODEL_PRIMARY=gemini-3.1-flash-lite
OCR_MODEL_FALLBACK=gemini-3.5-flash-lite
OCR_MODEL_QUALITY=
OCR_PROMPT_VERSION=2

# Optional technical safety controls. These are not usage quotas.
# Gemini Flash-Lite free-tier defaults deliberately stay below the observed 15 RPM ceiling.
OCR_MODEL_PRIMARY_RPM=12
OCR_MODEL_FALLBACK_RPM=12
OCR_PROVIDER_MAX_QUEUE_WAIT_MS=20000
# The frontend planner normally sends <=28 normal / <=14 dense pages and keeps an
# estimated 48k-token output budget. This server ceiling is a final safety guard.
OCR_BATCH_MAX_PAGES=28
OCR_BATCH_MAX_BYTES=12582912
OCR_REQUEST_TIMEOUT_MS=120000

# Google Drive OAuth — configure only as Supabase secrets
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_DRIVE_REDIRECT_URI=
GOOGLE_DRIVE_SCOPE=https://www.googleapis.com/auth/drive.file
GOOGLE_DRIVE_ROOT_FOLDER_NAME=Fichário Digital
