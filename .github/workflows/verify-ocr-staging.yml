name: Verify OCR staging

on:
  workflow_dispatch:
    inputs:
      confirm_external_ocr:
        description: Confirm one synthetic image may be sent to the configured OCR provider
        required: true
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: verify-ocr-staging
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    environment: staging
    env:
      OCR_STAGING_REPORT_PATH: /tmp/ocr-staging-report.json

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Initialize sanitized OCR report
        run: |
          cat > "$OCR_STAGING_REPORT_PATH" <<'JSON'
          {"schemaVersion":1,"status":"not_run","failureStage":"confirmation","stages":{"authenticated":false,"authorized":false,"consentRecorded":false,"importCreated":false,"functionCompleted":false,"persistenceVerified":false},"outcome":{"documentStatus":null,"pageStatus":null,"jobStatus":null,"needsReview":null,"warningCount":null,"attemptCount":null,"tokens":{"fichario":null,"ocr":null,"numericProbe":null}},"cleanup":{"document":"not_required","session":"not_required"}}
          JSON
          chmod 600 "$OCR_STAGING_REPORT_PATH"

      - name: Require explicit OCR confirmation
        env:
          CONFIRM_EXTERNAL_OCR: ${{ inputs.confirm_external_ocr }}
        run: test "$CONFIRM_EXTERNAL_OCR" = "true"

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.16.0
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify real OCR with a synthetic image
        env:
          STAGING_SUPABASE_URL: ${{ secrets.STAGING_SUPABASE_URL }}
          STAGING_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.STAGING_SUPABASE_PUBLISHABLE_KEY }}
          STAGING_AUTHORIZED_EMAIL: ${{ secrets.STAGING_AUTHORIZED_EMAIL }}
          STAGING_AUTHORIZED_PASSWORD: ${{ secrets.STAGING_AUTHORIZED_PASSWORD }}
        run: pnpm test:staging:ocr

      - name: Upload sanitized OCR report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ocr-staging-report-${{ github.run_id }}
          path: /tmp/ocr-staging-report.json
          if-no-files-found: error
          retention-days: 7
