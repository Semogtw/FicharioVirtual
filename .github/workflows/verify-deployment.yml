name: Verify deployed Fichário

on:
  workflow_dispatch:
    inputs:
      url:
        description: HTTPS origin to verify, without a path
        required: true
        type: string

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22.16.0
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify deployed site contract
        env:
          DEPLOYMENT_URL: ${{ inputs.url }}
        run: pnpm test:deployment -- "$DEPLOYMENT_URL"
